name: Deploy Dev

on:
  push:
    branches:
      - dev
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

concurrency:
  group: salomao-deploy-dev
  cancel-in-progress: false

jobs:
  deploy-dev:
    runs-on: self-hosted
    environment: dev

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Sincronizar checkout dev
        run: |
          SYNC_CHECKOUT_DIRTY_POLICY=reset bash scripts/sync-checkout-to-ref.sh /srv/salomao/dev/app dev

      - name: Deploy dev
        run: |
          bash /srv/salomao/dev/app/scripts/deploy-dev.sh

      - name: Healthcheck
        run: |
          sleep 3
          curl --fail --silent --show-error http://127.0.0.1:8101/api/v1/health
          echo
          echo "Deploy dev concluido com sucesso"

      - name: Archive Logs on Failure
        if: failure()
        shell: bash
        run: |
          (
            set +x
            mkdir -p debug-logs
            SERVICE_NAME="salomao-dev.service"
            APP_DIR="/srv/salomao/dev/app"
            systemctl status $SERVICE_NAME --no-pager > debug-logs/systemd-status.log 2>&1 || true
            ss -ltnp '( sport = :8101 )' > debug-logs/port-8101.log 2>&1 || true
            tail -n 200 "$APP_DIR/.runtime/backend.error.log" > debug-logs/backend-error.log 2>&1 || true
            tail -n 200 "$APP_DIR/.runtime/manual-dev-uvicorn.log" > debug-logs/manual-uvicorn.log 2>&1 || true
            sudo -n journalctl -u $SERVICE_NAME -n 500 --no-pager > debug-logs/systemd.log 2>&1 || true
            sudo -n tail -n 100 /var/log/nginx/error.log > debug-logs/nginx.log 2>&1 || true
          ) > /dev/null 2>&1

      - name: Upload Debug Artifact
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: deployment-debug-logs-dev-${{ github.run_id }}
          path: debug-logs/
          retention-days: 1
